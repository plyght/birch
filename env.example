# Birch API Configuration
#
# Copy this file to .env and configure for your environment.
# Required for: Birch SaaS API (apps/api)

# Supabase Configuration
# Get these values from your Supabase project settings
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# JWT Secret for validating Supabase JWT tokens (found in Project Settings > API > JWT Settings)
JWT_SECRET=your-jwt-secret

# Database Configuration
# PostgreSQL connection string (Supabase provides this in project settings)
DATABASE_URL=postgresql://postgres:password@localhost:54322/postgres

# Redis Configuration
# Used for credential caching and rate limiting
REDIS_URL=redis://localhost:6379

# API Configuration
# Host and port for the Birch API server
API_HOST=0.0.0.0
API_PORT=3000

# Encryption Keys
# Generate with: openssl rand -hex 32
# Used for envelope encryption of stored credentials
VAULT_MASTER_KEY=your-master-encryption-key-64-char-hex

# Logging
# Options: error, warn, info, debug, trace
RUST_LOG=info

